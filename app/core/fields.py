from __future__ import annotations

SCORE_FIELDS = [
    "score", "threat_score", "asset_context_score", "remediation_urgency_score",
    "decision", "decision_label", "sla_days", "target_date", "mitigation_required",
    "reasons", "policy_version", "policy_id", "first_seen_at", "first_scored_at", "last_scored_at",
]
