Place deployment-specific `fullchain.pem` and `privkey.pem` files in this directory
only on the target host. Certificate files are ignored by Git and must never be
committed or included in release archives.
