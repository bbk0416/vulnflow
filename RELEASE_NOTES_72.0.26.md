# VulnFlow 72.0.26

제어 DB의 독립 복구 경로를 추가하고 계정 전역 잠금 방식의 로그인 DoS 위험을 제거한 보안·복구 릴리스입니다.

## 주요 변경

- SQLite schema를 45로 올리고 기존 `failed_attempts`·`locked_until` 값을 초기화
- 계정 전체 잠금 대신 사용자명·클라이언트 조합과 클라이언트 전체 기준 sliding-window 실패 제한 적용
- 다른 출처의 공격 실패와 무관하게 올바른 비밀번호 사용자는 로그인 가능
- 존재하지 않는 계정, 비활성 계정, 잘못된 비밀번호와 제한된 요청의 외부 로그인 응답을 동일한 401 메시지로 통일
- 로그인 제한 시 `Retry-After`와 `Cache-Control: no-store` 응답 적용
- `control.db` 전용 오프라인 복구 번들 생성·검증·복원 CLI 추가
- 제어 DB 번들에서 세션, 로그인 시도 기록과 구형 잠금 상태 제외
- 복원 전 현재 제어 DB 자동 안전 백업과 실패 시 rollback 수행
- 복원 후 모든 세션·로그인 시도 기록 폐기
- 백업 이후 생성됐고 프로젝트 DB가 디스크에 남아 있는 프로젝트 등록정보 보존·병합
- DB 역할, SQLite 무결성, schema, 감사 체인, SHA-256과 선택 HMAC 서명 검증
- 프로젝트 범위가 필요한 저장 경로를 활성 프로젝트 없이 접근하면 실패하도록 강화
- 공개 핵심 회귀시험을 353개로 확대

## 운영 주의사항

제어 DB 복원은 웹 UI 기능이 아닙니다. 서비스를 완전히 중지한 뒤 `python -m scripts.manage_control_recovery`로 실행해야 합니다. 복원 성공 후 기존 브라우저 세션은 모두 무효화되므로 사용자는 다시 로그인해야 합니다.

HMAC 서명은 선택 사항입니다. 운영 백업에서 서명을 요구하려면 별도 관리한 키를 `--signing-key`, `--signing-key-id`, `--require-signature`와 함께 사용해야 합니다. 명령행 인자에 비밀을 직접 남기는 방식은 shell history에 노출될 수 있으므로 운영 자동화에서는 별도 비밀 주입 방식을 마련해야 합니다.

이 릴리스는 MFA·OIDC·SAML, 온라인 제어 DB 복원, 다중 노드 합의 복구를 제공하지 않습니다.
